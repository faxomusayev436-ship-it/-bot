services:
  - type: web
    name: my-bot
    env: python
    # 明确告诉它 requirements.txt 就在当前目录
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: API_TOKEN
        sync: false
